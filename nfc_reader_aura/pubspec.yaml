name: nfc_reader_aura
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2           # Untuk format tanggal dan waktu
  nfc_manager: ^3.5.0     # Library utama untuk membaca NFC

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
